
#include <iostream>
#include "Lib/Add.h"

int main() {
	int x = 2, y = 3;
	std::cout << x << " + " << y << " = " << add(x, y) << std::endl;
	std::cout << x << " + " << y << " = " << add(x, y) << std::endl;
	std::cout << x << " + " << y << " = " << add(x, y) << std::endl;
	return 0;
}
